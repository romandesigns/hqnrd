import puppeteer from "puppeteer"; // Corrected the typo here
import cheerio from "cheerio";

export const scrapeReviews = async (url: string) => {
  try {
  } catch (error) {
    console.log(error);
  }
};
