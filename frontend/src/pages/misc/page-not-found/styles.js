import styled from "styled-components";

export const PaseNotFoundStyled = styled.main`
  flex-grow: 1;
  display: flex;
  justify-content: center;
  align-items: center;
`;

export const ContentWrapper = styled.div`
  padding: 4rem;
  text-align: center;

  h1 {
    font-size: 1.5rem;
    margin: 2rem 0;
  }

  p {
    font-weight: 800;
    font-size: 3rem;
  }
`;
